import { styled, alpha } from '@mui/material/styles'

export const StyledMainViewContainer = styled('div')(({ theme }) => ({
  marginTop: theme.spacing(4),
  position: 'relative',
  width: '100%',
}))
